<!-- Main Navbar -->
<nav class="navbar navbar-expand-xl navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/home"><img src="/Assets/images/logo.png" id="logo">
      <h3 style="display: inline;" id="nav_Name"> Two Be Or Not Two Be</h3>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        <li class="nav-item moved">
          <% if (title=="Home" ) { %>
            <a class="nav-link active" href="/home">
              <% } else { %>
                <a class="nav-link" href="/home">
                  <% } %>
                    <i class="fas fa-home"></i> Home</a>
        </li>

        <li class="nav-item moved">
          <% if (title=="Surveys" ) { %>
            <a class="nav-link active" href="/survey-list">
              <% } else { %>
                <a class="nav-link" href="/survey-list">
                  <% } %><i class="fas fa-book"></i> Surveys</a>
        </li>
        <% if(typeof displayName=='string' && displayName) { %>
          <li class="navbar-text navbart"><span id="displayName"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></span>
          </li>
          
          <% } else { %>
             <% if (title=="Login" ) { %>
              <li class="nav-item navbart"><a href="/login" class="nav-link active">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
                <% } else { %>
                  <a class="nav-link navbart" href="/login"><i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
                  <% } %>
            <% } %>

            <div id="slide">
              <ul>
              <li><a href="/edit/"><i class="fas fa-edit"></i>Edit User Profile</a></li>
              <li><a href="/logout">
                <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li></li>
              </ul>
            </div>

    </div>
  </div>
</nav>